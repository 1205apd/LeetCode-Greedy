class Solution:
    def minDeletionSize(self, strs: List[str]) -> int:
        result = 0
        n = len(strs[0])        
        for i in range(n):
            for j in range(len(strs)-1):
                if strs[j][i] > strs[j+1][i]:
                    result += 1
                    break
        else:
            return result
                
                    
        
